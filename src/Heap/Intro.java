package Heap;

public class Intro {

    // Used to store elements:

   // Operations:
    // insertion - O(logn);
    // removal = O(logn);
    // min element = O(n);

    // how to know thw question is of heap ?
//    if kth (smallest, largest,closest ,most frequent )
//        if continuous sorting


//    for kth smallest => use maxHeap;
//    for kth largest => use minHeap;

}
